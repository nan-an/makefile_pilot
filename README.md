# A Pilot project to undestand Makefile based build systems.

## Expected capabilities
- Build C projects using just `Makefile`, no `CMake` or `Ninja`.
- Keep source files organized in `src` folder.
- Keep header files organized in `include` folder.
- Keep compiled object files and executables and libraries in `dist` folder.
- `TODO` Support for both static and shared libraries.
- `TODO` Support for multiple build configurations (e.g., Debug, Release).
- `TODO` Support for unit testing frameworks (e.g., Google Test/CTest).
